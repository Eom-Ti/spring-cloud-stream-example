dependencies {
    // docker compose auto start
    testAndDevelopmentOnly 'org.springframework.boot:spring-boot-docker-compose'

    // start exrate-functions module
    implementation project(':exrate-functions')
}
